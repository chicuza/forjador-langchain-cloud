# ============================================================================
# Forjador v5 - SPEC-01 MVP Environment Variables
# ============================================================================
# This file contains all 11 required environment variables for SPEC-01
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# LangSmith Tracing (Required)
# ----------------------------------------------------------------------------
# Enable LangSmith tracing for observability and monitoring
LANGCHAIN_TRACING_V2=true

# LangSmith API endpoint (default: https://api.smith.langchain.com)
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com

# Your LangSmith API key (get from https://smith.langchain.com/)
# Example: ls__1234567890abcdef1234567890abcdef
LANGCHAIN_API_KEY=your_langsmith_api_key_here

# LangSmith project name for organizing traces
# Recommended: forjador-v5-spec01 or forjador-v5-dev
LANGCHAIN_PROJECT=forjador-v5-spec01

# ----------------------------------------------------------------------------
# Gemini API (Required)
# ----------------------------------------------------------------------------
# Google Gemini API key for LLM and Vision capabilities
# Get from: https://aistudio.google.com/app/apikey
# Used for: SOL-05 (Gemini Vision) and structured extraction (B.6)
GOOGLE_API_KEY=your_google_api_key_here

# ----------------------------------------------------------------------------
# Language Settings
# ----------------------------------------------------------------------------
# Primary language for extraction and validation
# SPEC-01 uses Brazilian Portuguese (pt-BR)
# All extracted text and validation messages will use this language
PRIMARY_LANGUAGE=pt-BR

# ----------------------------------------------------------------------------
# Pipeline Settings (SPEC-01)
# ----------------------------------------------------------------------------
# Quality gate threshold (0.0-1.0) for B.4
# Documents with quality_score < threshold will be flagged
# Recommended: 0.85 (85% quality threshold)
QUALITY_GATE_THRESHOLD=0.85

# Chunk size for B.5 intelligent chunking (characters)
# Recommended: 3500 (optimal for Gemini context window)
CHUNK_SIZE=3500

# Chunk overlap for B.5 (characters)
# Recommended: 250 (maintains context between chunks)
CHUNK_OVERLAP=250

# ----------------------------------------------------------------------------
# File Queue Directories
# ----------------------------------------------------------------------------
# Input directory for documents to process
# Place PDF/Excel/CSV files here for processing
INPUT_DIR=./queue/input

# Output directory for processed results
# JSON and CSV outputs will be saved here
OUTPUT_DIR=./queue/output

# ----------------------------------------------------------------------------
# Optional: Additional Queue Directories (auto-created if needed)
# ----------------------------------------------------------------------------
# PROCESSING_DIR=./queue/processing  # Files being processed
# ERROR_DIR=./queue/error            # Failed files
# ARCHIVE_DIR=./queue/archive        # Successfully processed files

# ============================================================================
# Notes:
# ============================================================================
# - SPEC-01 is file-based only (NO database required)
# - All 11 variables above are REQUIRED for SPEC-01 to function
# - Performance target: < 25 seconds end-to-end for typical documents
# - Supports formats: PDF, Excel (.xlsx), CSV, images (PNG, JPG)
# - Outputs: JSON (structured data) + CSV (tabular format)
# - LangSmith tracing is mandatory for monitoring and debugging
# ============================================================================
